import Link from "next/link";import {updatePassword} from "@/app/actions/auth";export default async function AtualizarSenha({searchParams}:{searchParams:Promise<{erro?:string}>}){const p=await searchParams;return <div className="card form"><p className="eyebrow">PORTAL DOCENTE</p><h2>Crie uma nova senha</h2><p className="muted">Use pelo menos 8 caracteres e não compartilhe sua senha com ninguém.</p>{p.erro&&<div className="notice">{p.erro}</div>}<form action={updatePassword}><div className="field"><label>Nova senha</label><input name="password" type="password" minLength={8} autoComplete="new-password" required/></div><div className="field"><label>Confirmar nova senha</label><input name="confirmation" type="password" minLength={8} autoComplete="new-password" required/></div><button className="button">Atualizar senha</button></form><Link className="back-link" href="/login">Voltar para entrar</Link></div>}
