import Link from "next/link";import {requestPasswordReset} from "@/app/actions/auth";export default async function RecuperarSenha({searchParams}:{searchParams:Promise<{erro?:string;sucesso?:string}>}){const p=await searchParams;return <div className="card form"><p className="eyebrow">PORTAL DOCENTE</p><h2>Recuperar senha</h2><p className="muted">Informe seu e-mail. Enviaremos um link seguro para você criar uma nova senha.</p>{p.erro&&<div className="notice">{p.erro}</div>}{p.sucesso&&<div className="notice">{p.sucesso}</div>}<form action={requestPasswordReset}><div className="field"><label>E-mail</label><input name="email" type="email" autoComplete="email" required/></div><button className="button">Enviar link de recuperação</button></form><Link className="back-link" href="/login">Voltar para entrar</Link></div>}
