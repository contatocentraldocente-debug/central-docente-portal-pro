import {signIn} from "@/app/actions/auth";export default async function Login({searchParams}:{searchParams:Promise<{erro?:string;sucesso?:string}>}){const p=await searchParams;return <div className="card form"><h2>Entrar</h2>{p.erro&&<div className="notice">{p.erro}</div>}{p.sucesso&&<div className="notice">{p.sucesso}</div>}<form action={signIn}><div className="field"><label>E-mail</label><input name="email" type="email" required/></div><div className="field"><label>Senha</label><input name="password" type="password" required/></div><button className="button">Entrar</button></form></div>}