import {signUp} from "@/app/actions/auth";export default async function Cadastro({searchParams}:{searchParams:Promise<{erro?:string}>}){const p=await searchParams;return <div className="card form"><h2>Criar conta</h2>{p.erro&&<div className="notice">{p.erro}</div>}<form action={signUp}><div className="field"><label>Nome</label><input name="fullName" required/></div><div className="field"><label>E-mail</label><input name="email" type="email" required/></div><div className="field"><label>Senha</label><input name="password" type="password" minLength={8} required/></div><button className="button">Cadastrar</button></form></div>}