import {redirect} from "next/navigation";import {createClient} from "@/lib/supabase/server";export default async function Page(){const s=await createClient();const{data:{user}}=await s.auth.getUser();const{data:p}=await s.from("profiles").select("role").eq("id",user!.id).single();if(p?.role!=="admin")redirect("/dashboard");return <><h2>Administração</h2><div className="card">Gestão inicial de usuários e documentos.</div></>}