import {createClient} from "@/lib/supabase/server";export default async function Page(){const s=await createClient();const{data}=await s.from("documents").select("*").eq("is_active",true);return <><h2>Biblioteca</h2><div className="cards">{(data||[]).map((d:any)=><div className="card" key={d.id}><h3>{d.title}</h3><p className="muted">{d.category}</p><p>{d.description}</p></div>)}</div>{!data?.length&&<div className="card">Nenhum documento publicado.</div>}</>}