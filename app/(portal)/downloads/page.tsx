import {createClient} from "@/lib/supabase/server";export default async function Page(){const s=await createClient();const{data:{user}}=await s.auth.getUser();const{data}=await s.from("download_events").select("id,downloaded_at,documents(title,category)").eq("user_id",user!.id);return <><h2>Downloads</h2><div className="card">{data?.length?JSON.stringify(data):"Nenhum download registrado."}</div></>}