import {createClient} from "@/lib/supabase/server";export default async function Page(){const s=await createClient();const{data:{user}}=await s.auth.getUser();const{data:p}=await s.from("profiles").select("*").eq("id",user!.id).maybeSingle();return <><h2>Perfil</h2><div className="card"><p><b>Nome:</b> {p?.full_name||"—"}</p><p><b>E-mail:</b> {user?.email}</p><p><b>Perfil:</b> {p?.role||"teacher"}</p></div></>}