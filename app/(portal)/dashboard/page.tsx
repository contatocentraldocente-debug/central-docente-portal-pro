import {createClient} from "@/lib/supabase/server";export default async function Page(){const s=await createClient();const{data:{user}}=await s.auth.getUser();return <><h2>Painel do professor</h2><p className="muted">{user?.email}</p><div className="cards"><div className="card"><h3>Biblioteca</h3><p>Documentos pedagógicos.</p></div><div className="card"><h3>Downloads</h3><p>Histórico rastreável.</p></div><div className="card"><h3>Conta</h3><p>Acesso autenticado.</p></div></div></>}